# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=custom_admin
DB_PASSWORD=custom_admin
DB_NAME=custom_admin
DATABASE_URL="mysql://custom_admin:custom_admin@localhost:3306/custom_admin"

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random
JWT_EXPIRATION=24h
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here_make_it_long_and_random
JWT_REFRESH_EXPIRATION=7d

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5000

# Redis Configuration (optional for session management)
REDIS_HOST=localhost
REDIS_PORT=6379

# Security Configuration
BCRYPT_ROUNDS=12

# VITE Configuration
VITE_BACKEND_URL=http://localhost:5000/api/ 